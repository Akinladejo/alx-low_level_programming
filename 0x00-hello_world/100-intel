#include <stdio.h>

/**
* main - Entry point
*
*Return: Always 0 (Success)
*/
int main(void)
{
	CFILE=$1;
	OUTPUT_FILE=${CFILE%.c}.s;
	gcc -S -masm=intel $CFILE;
	cp $CFILE.s $OUTPUT_FILE;
	return (0);
}
